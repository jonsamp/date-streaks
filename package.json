{
  "name": "date-streaks",
  "version": "1.2.1",
  "description": "Find a variety of streak metrics from a list of dates.",
  "main": "./dist/index.js",
  "scripts": {
    "test": "npm run build && mocha test/*.js",
    "build": "babel src --presets babel-preset-env --out-dir dist",
    "prepublish": "npm run build"
  },
  "keywords": [
    "streak",
    "streaks",
    "date",
    "days",
    "dates",
    "day"
  ],
  "author": "Jon Samp",
  "license": "ISC",
  "repository": "https://github.com/jonsamp/date-streaks",
  "bugs": {
    "url": "https://github.com/jonsamp/date-streaks/issues"
  },
  "dependencies": {
    "date-fns": "1.30.1"
  },
  "devDependencies": {
    "babel-cli": "6.26.0",
    "babel-plugin-transform-object-rest-spread": "6.26.0",
    "babel-preset-env": "1.6.1",
    "chai": "4.1.2",
    "mocha": "5.0.5"
  }
}
